@model IEnumerable<ET.Ministries>

@{ Layout = null; }


@foreach (var item in Model)
    {
        <li>@Html.ActionLink(item.Name, item.ActionLink, "Ministries", routeValues: null, htmlAttributes: new { @class = "text-black text-decoration-none" })</li>
    }                    